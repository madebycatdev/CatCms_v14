@model IList<EuroCMS.Admin.entity.cms_asp_admin_select_css_revisions_Result>
 
<!-- BEGIN SAMPLE TABLE PORTLET-->
<div class="row-fluid">
    <div class="span12">
        <div class="portlet box grey">
            <div class="portlet-title">
                <div class="caption"><i class="icon-reorder"></i>Revisions List</div>
            </div>
            <div class="portlet-body">
                <table class="table table-striped table-hover">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Created By</th>
                            <th>Actions </th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model.Count > 0)
                        { 
                            foreach (var _css in Model)
                            { 
                                <tr>
                                    <td> @_css.history_id </td>
                                    <td> @_css.created by @_css.publisher_name </td>
                                    <td> 
                                        <a href="@Url.Action("Edit", new { id= @_css.css_id, RevisionId =_css.history_id })" class="btn icn-only black"><i class="icon-eye-open"></i></a>
                                        @*<a href="@Url.Action("Delete", new { id= @_css.css_id})" class="btn icn-only black" onclick="deleteObject(this); return false;" data-message="Are you sure want to delete this layout?"><i class="icon-trash"></i></a>*@
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr><td colspan="4">No Result Found!</td></tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
<!-- END SAMPLE TABLE PORTLET-->

